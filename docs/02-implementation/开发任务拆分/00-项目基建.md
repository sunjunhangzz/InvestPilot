# 00-项目基建

## 任务 00.1：确认目录结构

所属子项目：全局

前置依赖：无

开发内容：

- 确认 `app/web`
- 确认 `app/worker`
- 确认 `app/shared`
- 确认 `data`
- 确认 `reports`
- 确认 `docs`

验收标准：

- 所有目录存在。
- 文档索引能说明目录用途。

风险点：

- 目录含义不清，后续文件乱放。

## 任务 00.2：确认 shared 配置

所属子项目：shared

前置依赖：00.1

开发内容：

- 检查 `app/shared/config.json`
- 检查 `app/shared/schema.json`
- 确认数据库路径
- 确认报告路径
- 确认推荐数量
- 确认因子权重

验收标准：

- `config.json` 是合法 JSON。
- `schema.json` 是合法 JSON。
- 数据库路径指向 `data/a_stock_research.sqlite`。
- 所有相对路径都按项目根目录解析。
- 因子权重总和为 1。

风险点：

- 相对路径从不同工作目录读取时可能失效。

必须通过统一路径解析工具解决该风险，禁止在 web 和 worker 中各自手写路径拼接。

## 任务 00.3：创建 Python 环境说明

所属子项目：worker

前置依赖：00.2

开发内容：

- 创建 `requirements.txt`
- 写入第一版依赖
- 说明如何创建虚拟环境

验收标准：

- 能用 Python 3.12 创建虚拟环境。
- 能安装依赖。

风险点：

- AkShare 安装或运行依赖网络。

## 任务 00.3a：创建 worker 源码骨架

所属子项目：worker

前置依赖：00.3

开发内容：

- 创建 `app/worker/src/config/`
- 创建 `app/worker/src/db/`
- 创建 `app/worker/src/data_sources/`
- 创建 `app/worker/src/factors/`
- 创建 `app/worker/src/screening/`
- 创建 `app/worker/src/watchlist/`
- 创建 `app/worker/src/reports/`
- 创建 `app/worker/src/tasks/`
- 创建 `app/worker/src/utils/`

验收标准：

- worker 目录结构和架构文档一致。
- 脚本只负责入口编排，业务逻辑放到 `src/` 下。

风险点：

- 如果所有逻辑都写在 scripts 里，后续会难测试和复用。

## 任务 00.4：创建 Web 项目骨架

所属子项目：web

前置依赖：00.2

开发内容：

- 初始化 Next.js 项目
- 配置 TypeScript
- 配置 Tailwind CSS
- 创建基础页面目录
- 创建 `components/`
- 创建 `lib/`
- 创建 `types/`

验收标准：

- `npm run dev` 能启动。
- 浏览器能打开首页。
- web 目录结构和架构文档一致。

风险点：

- 依赖安装需要联网。

## 任务 00.5：创建基础工程文件

所属子项目：全局

前置依赖：00.1

开发内容：

- 创建根目录 `.gitignore`
- 创建根目录 `.env.example`
- 创建 `app/worker/requirements.txt`
- 创建本地启动说明

验收标准：

- `.gitignore` 排除 `node_modules`、`.venv`、数据库文件、缓存、日志、`.env`
- `.env.example` 不包含真实密钥
- `requirements.txt` 包含第一版 Python 依赖
- 新开发者能根据文档启动项目

风险点：

- 误把 API Key、数据库、缓存文件提交进版本管理

## 任务 00.6：创建日志目录约定

所属子项目：全局

前置依赖：00.1

开发内容：

- 创建 `data/logs/`
- 约定 worker 日志文件位置
- 约定 API 日志和任务错误记录方式

验收标准：

- 数据采集失败能在日志中定位
- AI 调用失败能在日志中定位
- 任务失败能在 `system_tasks.error_message` 中看到摘要

风险点：

- 没有日志时，数据源或任务失败难以排查

## 任务 00.7：创建日志工具约定

所属子项目：worker / web

前置依赖：00.6

开发内容：

- worker 统一日志格式。
- API 统一日志格式。
- 日志写入 `data/logs/`。
- 日志使用 JSON Lines 格式。

验收标准：

- worker 能写 `worker.log`。
- API 能写 `api.log`。
- 日志包含 `task_id`、`run_id`、`level`、`module`、`message`。

风险点：

- 各模块各写各的日志格式，后续难以收集。

## 基建阶段复盘风险

完成 `00-项目基建` 后保留以下风险记录，后续开发需要持续关注：

1. `app/web/app/api/health/log/route.ts` 是本地日志健康检查接口。
   - 本地开发可保留。
   - 如果后续部署到外网，必须限制访问或删除，避免被外部请求反复写日志。

2. Web 当前使用 Next.js 16 和 Turbopack。
   - `@shared/*` 依赖 `next.config.ts` 中的 `turbopack.root` 和 `turbopack.resolveAlias`。
   - 后续升级 Next.js 时，必须回归验证 `@shared/paths` 是否还能正常解析。

3. 本地运行产物会持续存在。
   - 包括 `.next/`、`node_modules/`、`.venv/`、`__pycache__/`、`data/logs/*.log`。
   - 这些文件已被 `.gitignore` 忽略，不应提交。

4. Python worker 从不同目录执行时，必须确认项目根目录在导入路径中。
   - 后续 `scripts/*.py` 入口需要统一处理 `sys.path` 或采用统一启动方式。
   - 禁止每个脚本各自临时拼接配置路径。

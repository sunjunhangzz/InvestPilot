# api 子项目

## 定位

`api` 是后续独立后端的预留目录。

第一版不启用独立 API 服务，API 由 `app/web` 的 Next.js API Routes 承担。

## 什么时候启用

当系统出现以下情况时，再考虑启用独立后端：

- API Routes 难以管理复杂任务。
- 需要长期运行的任务队列。
- 需要多用户和权限系统。
- 需要部署到服务器。
- 需要 PostgreSQL、Redis、消息队列等基础设施。

## 第一版不做

第一版不在这里写代码。

